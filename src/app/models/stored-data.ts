export class StoredData {
    name: string;
    extra: string;
    count: number;
    constructor(name: string, extra: string, count: number) {
        this.name = name;
        this.extra = extra;
        this.count = count;
    }
}
